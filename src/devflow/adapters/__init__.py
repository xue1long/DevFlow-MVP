"""DevFlow 适配层 · v0.3 (A2 — 纪律约束)

================================================================================
适配层 v0.3 纪律（建立秩序 · 根治源头风险）
================================================================================

【禁止】
1. ❌ 任何手写 Skill manifest —— 必须从 cli.py 自动派生
   （理由：v0.3 INDEX 教训，manifest 漂移是 4 角色交叉 P0）
2. ❌ 在适配层加业务逻辑 —— 适配层只做协议转换
3. ❌ 自建 Agent 平台接口 —— devflow 编排 Agent，不实现 Agent
4. ❌ 跨平台分支逻辑 —— 平台差异在 detect() 层处理，调用方无感

【允许】
1. ✅ EngineInvoker 抽象 —— 统一 Skill/MCP/CLI 调用面
2. ✅ detect() 平台探测 —— 必须先于路由
3. ✅ Type 注解 + Pydantic —— manifest 是 schema，不允许裸 dict

【执行节奏】
- 第 1 周：仅做 A+C（cli.py 契约注释 + json_schema + EngineInvoker + manifest 派生）
- B 阶段（MCP Server / SDD 编排 / 三平台适配）择机重启，触发条件见 v0.3 方案

================================================================================
"""